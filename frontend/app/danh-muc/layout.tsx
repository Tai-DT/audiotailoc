import { metadata as categoriesMetadata } from '../categories/layout';

export const metadata = categoriesMetadata;

export default function CategoriesAliasLayout({
 children,
}: {
 children: React.ReactNode;
}) {
 return children;
}
