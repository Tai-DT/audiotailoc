# 📋 Các Biến Môi Trường Đã Xóa (Không Được Sử Dụng)

## ✅ Đã Xóa:

1. **POSTGRES_URL** - Không được sử dụng (chỉ dùng DATABASE_URL)
2. **DIRECT_DATABASE_URL** - Không được sử dụng
3. **API_VERSION** - Không được sử dụng
4. **EMAIL_FROM** - Không được sử dụng (chỉ dùng SMTP_FROM)
5. **CLOUDINARY_UPLOAD_PRESET** - Không được sử dụng
6. **MINIO_*** - Không được sử dụng (MINIO_ENDPOINT, MINIO_PORT, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_BUCKET_NAME)
7. **GOOGLE_AI_API_KEY** - Không được sử dụng (chỉ dùng GOOGLE_GEMINI_API_KEY)
8. **GEMINI_MODEL** - Không được sử dụng
9. **MEILI_*** - Không được sử dụng (MEILI_MASTER_KEY, MEILI_URL)
10. **THROTTLE_*** - Không được sử dụng (THROTTLE_TTL, THROTTLE_LIMIT)
11. **LOG_FILE** - Không được sử dụng
12. **BCRYPT_ROUNDS** - Không được sử dụng
13. **SESSION_SECRET** - Không được sử dụng (chỉ dùng SESSION_TIMEOUT)
14. **PAYOS_WEBHOOK_URL** - Không được đọc trong code (chỉ để cấu hình trên PayOS dashboard / tài liệu)
15. **PAYOS_RETURN_URL** - Không được đọc trong code (frontend/clients truyền `returnUrl` khi tạo intent)
16. **PAYOS_CANCEL_URL** - Không được đọc trong code (frontend/clients truyền `cancelUrl` khi tạo link, nếu có)
17. **VNPAY_RETURN_URL** - Không được sử dụng trong code
18. **TELEGRAM_WEBHOOK_URL** - Không được sử dụng trong code

## ✅ Đã Giữ Lại (Được Sử Dụng):

- DATABASE_URL
- JWT_ACCESS_SECRET, JWT_REFRESH_SECRET
- PAYOS_CLIENT_ID, PAYOS_API_KEY, PAYOS_CHECKSUM_KEY, PAYOS_PARTNER_CODE, PAYOS_API_URL, PAYOS_ALLOW_TEST_WEBHOOK
- VNPAY_TMN_CODE, VNPAY_HASH_SECRET, VNPAY_PAY_URL, VNPAY_REFUND_URL
- MOMO_PARTNER_CODE, MOMO_ACCESS_KEY, MOMO_SECRET_KEY, MOMO_ENDPOINT, MOMO_REFUND_ENDPOINT
- PORT, NODE_ENV
- REDIS_URL, REDIS_PASSWORD, REDIS_DB
- SMTP_HOST, SMTP_PORT, SMTP_SECURE, SMTP_USER, SMTP_PASS, SMTP_FROM
- CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET
- FRONTEND_URL, DASHBOARD_URL
- TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_IDS
- LOG_LEVEL
- ADMIN_API_KEY
- SESSION_TIMEOUT
- CORS_ORIGIN
- GOOGLE_GEMINI_API_KEY
- GOONG_API_KEY
- FEATURE_CHECKOUT
- SHIPPING_FLAT_CENTS

