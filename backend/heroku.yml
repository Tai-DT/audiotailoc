build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false
run:
  web:
    command: |
      npx prisma migrate deploy && npm start
    env:
      NODE_ENV: production
