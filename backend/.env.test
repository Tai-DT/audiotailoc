# Test Environment Variables
NODE_ENV=test
DATABASE_URL="postgresql://test:test@localhost:5432/audiotailoc_test"
DIRECT_DATABASE_URL="postgresql://test:test@localhost:5432/audiotailoc_test"

# JWT Secrets for testing
JWT_ACCESS_SECRET=test-jwt-access-secret-key-for-testing-only
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key-for-testing-only
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Cloudinary (mock for testing)
CLOUDINARY_CLOUD_NAME=test-cloud
CLOUDINARY_API_KEY=test-key
CLOUDINARY_API_SECRET=test-secret

# Redis (mock for testing)
REDIS_URL=redis://localhost:6379/1

# Email (mock for testing)
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USER=test
EMAIL_PASS=test
EMAIL_FROM=test@example.com

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File upload limits
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx

# Application settings
PORT=3010
API_PREFIX=/api/v1
CORS_ORIGIN=http://localhost:3000,http://localhost:3001