# Audio Tài Lộc - System Architecture & Data Flow

## 📋 Tổng quan hệ thống

Audio Tài Lộc được xây dựng theo kiến trúc **3-tier** với các thành phần chính:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENTS (Browser)                               │
├───────────────────────────────┬─────────────────────────────────────────────┤
│         FRONTEND              │              DASHBOARD                       │
│    (Next.js - Port 3000)      │         (Next.js - Port 3001)               │
│    Khách hàng truy cập        │         Admin quản lý                        │
└───────────────┬───────────────┴─────────────────────┬───────────────────────┘
                │                                     │
                │  HTTP/REST API                      │  HTTP/REST API
                │                                     │
                ▼                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          BACKEND (NestJS - Port 3010)                        │
│                                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │  Products   │  │  Services   │  │   Orders    │  │   Authentication   │ │
│  │   Module    │  │   Module    │  │   Module    │  │      Module        │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────┬───────────────────────────────────┘
                                          │
                                          │  Prisma ORM
                                          ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         DATABASE (PostgreSQL)                                │
│                                                                              │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│   │ products │  │ services │  │  orders  │  │  users   │  │   reviews    │ │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔗 Chi tiết kết nối

### Environment Variables

| Component   | Variable                 | Value                      |
|-------------|--------------------------|----------------------------|
| Frontend    | `NEXT_PUBLIC_API_URL`    | `http://localhost:3010/api/v1` |
| Dashboard   | `NEXT_PUBLIC_API_URL`    | `http://localhost:3010/api/v1` |
| Dashboard   | `NEXT_PUBLIC_ADMIN_API_KEY` | `admin-secret-key`      |
| Backend     | `DATABASE_URL`           | `postgresql://...`         |
| Backend     | `JWT_SECRET`             | `your-jwt-secret`          |

---

## 📦 Data Flow: Sản phẩm (Products)

### 1. Tạo sản phẩm mới (Dashboard → Backend → Database)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         CREATE PRODUCT FLOW                                   │
└──────────────────────────────────────────────────────────────────────────────┘

Dashboard (Admin)                   Backend                        Database
     │                                 │                               │
     │  1. Upload images               │                               │
     │ ─────────────────────────────►  │                               │
     │  POST /api/upload               │                               │
     │  (→ Cloudinary)                 │                               │
     │  ◄───────────────────────────── │                               │
     │  Return: image URLs             │                               │
     │                                 │                               │
     │  2. Submit product form         │                               │
     │ ─────────────────────────────►  │                               │
     │  POST /api/v1/products          │                               │
     │  Headers:                       │                               │
     │    x-api-key: admin-key         │                               │
     │  Body: {                        │                               │
     │    name, slug, price,           │                               │
     │    images: [...urls],           │  3. Validate & Transform      │
     │    categoryId, ...              │ ─────────────────────────────►│
     │  }                              │  INSERT INTO products         │
     │                                 │  ◄─────────────────────────── │
     │  ◄───────────────────────────── │  Return: product record       │
     │  Response: { success, data }    │                               │
     │                                 │                               │
     ▼                                 ▼                               ▼
```

### 2. Hiển thị sản phẩm (Frontend ← Backend ← Database)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         GET PRODUCTS FLOW                                     │
└──────────────────────────────────────────────────────────────────────────────┘

Frontend (Customer)                 Backend                        Database
     │                                 │                               │
     │  1. User visits /products       │                               │
     │ ─────────────────────────────►  │                               │
     │  GET /api/v1/products           │                               │
     │  Query: ?page=1&limit=12        │  2. Query database            │
     │         &category=speakers      │ ─────────────────────────────►│
     │                                 │  SELECT * FROM products       │
     │                                 │  WHERE category = '...'       │
     │                                 │  LIMIT 12 OFFSET 0            │
     │                                 │  ◄─────────────────────────── │
     │                                 │  Return: products[]           │
     │  ◄───────────────────────────── │                               │
     │  Response: {                    │  3. Parse JSON fields         │
     │    items: [...products],        │     (images, specifications)  │
     │    total: 50,                   │                               │
     │    page: 1,                     │                               │
     │    totalPages: 5                │                               │
     │  }                              │                               │
     │                                 │                               │
     │  4. Render ProductGrid          │                               │
     │     with images from            │                               │
     │     Cloudinary CDN              │                               │
     ▼                                 ▼                               ▼
```

---

## 🔧 Data Flow: Dịch vụ (Services)

### 1. Tạo dịch vụ (Dashboard → Backend)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         CREATE SERVICE FLOW                                   │
└──────────────────────────────────────────────────────────────────────────────┘

Dashboard                           Backend                        Database
     │                                 │                               │
     │  POST /api/v1/services          │                               │
     │  Body: {                        │                               │
     │    name: "Tư vấn Hi-End",       │                               │
     │    slug: "tu-van-hi-end",       │                               │
     │    price: 5000000,              │                               │
     │    duration: 120,          ───► │  Transform:                   │
     │    images: ["url1", "url2"],    │  - duration → estimatedDuration
     │    features: ["f1", "f2"],      │  - images[] → JSON string     │
     │    requirements: ["r1"],        │  - features[] → JSON string   │
     │    tags: ["tag1", "tag2"]       │  - requirements[] → JSON string
     │  }                              │  - tags[] → JSON string       │
     │                                 │ ─────────────────────────────►│
     │                                 │  INSERT INTO services (       │
     │                                 │    images = '["url1","url2"]' │
     │                                 │  )                            │
     │  ◄───────────────────────────── │  ◄─────────────────────────── │
     │  { success: true, data: {...} } │                               │
     ▼                                 ▼                               ▼
```

### 2. Đặt lịch dịch vụ (Frontend → Backend)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         SERVICE BOOKING FLOW                                  │
└──────────────────────────────────────────────────────────────────────────────┘

Frontend                            Backend                        Database
     │                                 │                               │
     │  User: Guest or Authenticated   │                               │
     │                                 │                               │
     ├── IF Guest ─────────────────────┤                               │
     │   POST /api/v1/bookings/guest   │                               │
     │   Body: {                       │                               │
     │     serviceId,                  │                               │
     │     customerName,               │                               │
     │     customerPhone,              │                               │
     │     customerEmail,              │                               │
     │     scheduledDate,              │                               │
     │     notes                       │                               │
     │   }                             │                               │
     │                                 │                               │
     ├── IF Authenticated ─────────────┤                               │
     │   POST /api/v1/bookings         │                               │
     │   Headers: Authorization        │                               │
     │   Body: {                       │                               │
     │     serviceId,                  │                               │
     │     userId,     ◄── auto-filled │                               │
     │     scheduledDate,              │                               │
     │   }                             │                               │
     │                                 │ ─────────────────────────────►│
     │                                 │  INSERT INTO service_bookings │
     │  ◄───────────────────────────── │  ◄─────────────────────────── │
     │  Booking confirmation           │                               │
     ▼                                 ▼                               ▼
```

---

## 🛒 Data Flow: Đơn hàng (Orders)

### 1. Quy trình đặt hàng

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         ORDER CREATION FLOW                                   │
└──────────────────────────────────────────────────────────────────────────────┘

Frontend                            Backend                        Database
     │                                 │                               │
     │  1. Add to Cart (localStorage)  │                               │
     │  ════════════════════════════   │                               │
     │  Cart: [                        │                               │
     │    { productId, qty, variant }  │                               │
     │  ]                              │                               │
     │                                 │                               │
     │  2. Checkout                    │                               │
     │ ─────────────────────────────►  │                               │
     │  POST /api/v1/orders            │                               │
     │  Body: {                        │                               │
     │    items: [...cart],            │  3. Validate:                 │
     │    shippingAddress: {...},      │  - Check product stock        │
     │    paymentMethod: "COD",        │  - Calculate totals           │
     │    customerInfo: {...}          │  - Verify prices              │
     │  }                              │ ─────────────────────────────►│
     │                                 │  BEGIN TRANSACTION            │
     │                                 │  INSERT INTO orders           │
     │                                 │  INSERT INTO order_items      │
     │                                 │  UPDATE products (stock)      │
     │                                 │  COMMIT                       │
     │  ◄───────────────────────────── │  ◄─────────────────────────── │
     │  { orderId, status: "pending" } │                               │
     │                                 │                               │
     │  4. Clear cart                  │                               │
     │  5. Redirect to order success   │                               │
     ▼                                 ▼                               ▼
```

### 2. Quản lý đơn hàng (Dashboard)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         ORDER MANAGEMENT FLOW                                 │
└──────────────────────────────────────────────────────────────────────────────┘

Dashboard                           Backend                        Database
     │                                 │                               │
     │  1. View Orders List            │                               │
     │ ─────────────────────────────►  │                               │
     │  GET /api/v1/orders             │ ─────────────────────────────►│
     │  Query: ?status=pending         │  SELECT * FROM orders         │
     │                                 │  WHERE status = 'pending'     │
     │  ◄───────────────────────────── │  ◄─────────────────────────── │
     │                                 │                               │
     │  2. Update Order Status         │                               │
     │ ─────────────────────────────►  │                               │
     │  PATCH /api/v1/orders/:id       │                               │
     │  Body: { status: "confirmed" }  │ ─────────────────────────────►│
     │                                 │  UPDATE orders                │
     │                                 │  SET status = 'confirmed'     │
     │  ◄───────────────────────────── │  ◄─────────────────────────── │
     │                                 │                               │
     │  3. Dashboard updates UI        │                               │
     ▼                                 ▼                               ▼
```

---

## 🔐 Authentication Flow

### 1. Đăng nhập người dùng (Frontend)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         USER LOGIN FLOW                                       │
└──────────────────────────────────────────────────────────────────────────────┘

Frontend                            Backend                        Database
     │                                 │                               │
     │  POST /api/v1/auth/login        │                               │
     │  Body: { email, password }      │                               │
     │                                 │ ─────────────────────────────►│
     │                                 │  SELECT * FROM users          │
     │                                 │  WHERE email = '...'          │
     │                                 │  ◄─────────────────────────── │
     │                                 │                               │
     │                                 │  Verify password (bcrypt)     │
     │                                 │  Generate JWT token           │
     │  ◄───────────────────────────── │                               │
     │  {                              │                               │
     │    accessToken: "eyJ...",       │                               │
     │    user: { id, name, email }    │                               │
     │  }                              │                               │
     │                                 │                               │
     │  Store in localStorage/cookie   │                               │
     │  Set Authorization header       │                               │
     ▼                                 ▼                               ▼
```

### 2. Dashboard Admin Authentication

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         ADMIN AUTH FLOW                                       │
└──────────────────────────────────────────────────────────────────────────────┘

Dashboard                           Backend                        Database
     │                                 │                               │
     │  All API Requests include:      │                               │
     │  Headers: {                     │                               │
     │    "x-api-key": "admin-key"     │                               │
     │  }                              │                               │
     │ ─────────────────────────────►  │                               │
     │                                 │  Middleware: ApiKeyGuard      │
     │                                 │  - Validate x-api-key         │
     │                                 │  - Check admin permissions    │
     │                                 │                               │
     │  If valid ─────────────────────►│  Process request              │
     │  If invalid ◄───────────────────│  401 Unauthorized             │
     ▼                                 ▼                               ▼
```

---

## 🖼️ Image Upload Flow

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         IMAGE UPLOAD FLOW                                     │
└──────────────────────────────────────────────────────────────────────────────┘

Dashboard/Frontend                  Dashboard API Route             Cloudinary
     │                                 │                               │
     │  1. Select file(s)              │                               │
     │  2. Validate (size, type)       │                               │
     │                                 │                               │
     │  POST /api/upload               │                               │
     │  Body: FormData with file       │                               │
     │ ─────────────────────────────►  │                               │
     │                                 │  3. Generate signature        │
     │                                 │  4. Upload to Cloudinary      │
     │                                 │ ─────────────────────────────►│
     │                                 │                               │
     │                                 │  ◄─────────────────────────── │
     │                                 │  Return: { url, public_id }   │
     │  ◄───────────────────────────── │                               │
     │  {                              │                               │
     │    url: "https://res.cloud...", │                               │
     │    publicId: "products/abc123"  │                               │
     │  }                              │                               │
     │                                 │                               │
     │  5. Use URL in form submission  │                               │
     ▼                                 ▼                               ▼
```

---

## 📊 Database Schema Overview

```sql
-- Core Tables
┌─────────────────────────────────────────────────────────────────────┐
│  products                    │  services                            │
├──────────────────────────────┼──────────────────────────────────────┤
│  id (UUID)                   │  id (UUID)                           │
│  name                        │  name                                │
│  slug                        │  slug                                │
│  price                       │  price                               │
│  images (JSON String)        │  images (JSON String)                │
│  categoryId → categories     │  serviceTypeId → service_types       │
│  specifications (JSON)       │  features (JSON String)              │
│  stock                       │  requirements (JSON String)          │
│  isActive                    │  estimatedDuration                   │
│  createdAt                   │  isActive                            │
│  updatedAt                   │  createdAt                           │
└──────────────────────────────┴──────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  orders                      │  service_bookings                    │
├──────────────────────────────┼──────────────────────────────────────┤
│  id (UUID)                   │  id (UUID)                           │
│  orderNumber                 │  bookingNumber                       │
│  userId → users              │  userId → users (optional)           │
│  status                      │  serviceId → services                │
│  totalAmount                 │  customerName                        │
│  items → order_items         │  customerPhone                       │
│  shippingAddress (JSON)      │  customerEmail                       │
│  paymentMethod               │  scheduledAt                         │
│  createdAt                   │  status                              │
└──────────────────────────────┴──────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  users                       │  reviews                             │
├──────────────────────────────┼──────────────────────────────────────┤
│  id (UUID)                   │  id (UUID)                           │
│  email                       │  userId → users                      │
│  password (hashed)           │  productId → products                │
│  name                        │  rating (1-5)                        │
│  phone                       │  comment                             │
│  role (USER/ADMIN)           │  status (pending/approved)           │
│  isActive                    │  createdAt                           │
└──────────────────────────────┴──────────────────────────────────────┘
```

---

## 🔄 State Management

### Frontend (React Query / SWR)
```typescript
// Fetching data with caching
const { data, isLoading, error } = useProducts(filters);

// Mutations with cache invalidation
const mutation = useMutation({
  mutationFn: createOrder,
  onSuccess: () => {
    queryClient.invalidateQueries(['cart']);
  }
});
```

### Dashboard (React Query)
```typescript
// Admin data fetching
const { data: orders } = useOrders({ status: 'pending' });

// CRUD operations
const updateOrder = useMutation({
  mutationFn: (data) => orderService.update(id, data),
  onSuccess: () => {
    queryClient.invalidateQueries(['orders']);
    toast.success('Cập nhật thành công');
  }
});
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              PRODUCTION                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────────────────────┐ │
│  │   Vercel    │      │   Vercel    │      │        Railway/Render       │ │
│  │  Frontend   │      │  Dashboard  │      │          Backend            │ │
│  │  (Next.js)  │      │  (Next.js)  │      │          (NestJS)           │ │
│  └──────┬──────┘      └──────┬──────┘      └──────────────┬──────────────┘ │
│         │                    │                            │                 │
│         │                    │                            │                 │
│         └────────────────────┴────────────────────────────┘                 │
│                              │                                               │
│                              ▼                                               │
│                    ┌─────────────────┐                                       │
│                    │   Supabase/     │                                       │
│                    │   Neon (PgSQL)  │                                       │
│                    └─────────────────┘                                       │
│                                                                              │
│  External Services:                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                       │
│  │  Cloudinary  │  │    Resend    │  │   Stripe     │                       │
│  │   (Images)   │  │   (Email)    │  │  (Payment)   │                       │
│  └──────────────┘  └──────────────┘  └──────────────┘                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 Project Structure

```
audiotailoc/
├── backend/                    # NestJS API Server
│   ├── src/
│   │   ├── modules/           # Feature modules
│   │   │   ├── products/      # Products CRUD
│   │   │   ├── services/      # Services CRUD
│   │   │   ├── orders/        # Orders management
│   │   │   ├── booking/       # Service bookings
│   │   │   ├── auth/          # Authentication
│   │   │   └── ...
│   │   ├── common/            # Shared utilities
│   │   └── main.ts            # Entry point
│   └── prisma/
│       └── schema.prisma      # Database schema
│
├── frontend/                   # Next.js Customer Website
│   ├── app/                   # App Router pages
│   │   ├── products/          # Product pages
│   │   ├── services/          # Service pages
│   │   ├── cart/              # Shopping cart
│   │   └── ...
│   ├── components/            # React components
│   ├── lib/
│   │   ├── hooks/             # Custom hooks (useProducts, useServices, etc.)
│   │   └── services/          # API service layer
│   └── ...
│
└── dashboard/                  # Next.js Admin Dashboard
    ├── app/
    │   └── dashboard/         # Admin pages
    │       ├── products/      # Product management
    │       ├── services/      # Service management
    │       ├── orders/        # Order management
    │       └── ...
    ├── components/            # Admin components
    ├── lib/
    │   ├── services/          # API service layer
    │   └── api-client.ts      # API configuration
    └── ...
```

---

## 🔑 Key API Endpoints

| Category | Endpoint | Method | Auth | Description |
|----------|----------|--------|------|-------------|
| **Products** | `/products` | GET | No | List products |
| | `/products/:slug` | GET | No | Product detail |
| | `/products` | POST | Admin | Create product |
| | `/products/:id` | PUT | Admin | Update product |
| | `/products/:id` | DELETE | Admin | Delete product |
| **Services** | `/services` | GET | No | List services |
| | `/services/:slug` | GET | No | Service detail |
| | `/services` | POST | Admin | Create service |
| **Orders** | `/orders` | POST | Optional | Create order |
| | `/orders` | GET | Admin | List orders |
| | `/orders/:id` | PATCH | Admin | Update order |
| **Bookings** | `/bookings/guest` | POST | No | Guest booking |
| | `/bookings` | POST | User | Auth booking |
| **Auth** | `/auth/login` | POST | No | Login |
| | `/auth/register` | POST | No | Register |

---

*Document created: 2025-12-27*
*Version: 1.0*
